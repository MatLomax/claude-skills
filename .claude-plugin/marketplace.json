{
  "name": "claude-skills",
  "owner": { "name": "Mathieu Lomax" },
  "plugins": [
    {
      "name": "image-to-html",
      "source": "./plugins/image-to-html",
      "description": "Reconstruct HTML from a mockup image and objectively gate the render against it (per-region SSIM, edge-XOR, colour sweeps, glyph measurement).",
      "defaultEnabled": false
    },
    {
      "name": "worklog",
      "source": "./plugins/worklog",
      "description": "Per-project, SQLite-backed task & decision log for coding agents, remembered across sessions. MCP server + SessionStart/Stop hooks + /worklog:init. Requires the `worklog` binary on PATH.",
      "defaultEnabled": false
    }
  ]
}
